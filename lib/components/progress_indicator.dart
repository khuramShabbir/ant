import 'package:flutter/material.dart';

class ShowCircularProgress extends StatefulWidget {
  const ShowCircularProgress({Key? key}) : super(key: key);

  @override
  State<ShowCircularProgress> createState() => _ShowCircularProgressState();
}

class _ShowCircularProgressState extends State<ShowCircularProgress> {
  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
